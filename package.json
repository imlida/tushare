{
  "name": "tushare-api-client",
  "version": "1.0.0",
  "description": "Tushare API TypeScript客户端，支持获取股票市场分钟、日、周、月、年级别的K线数据",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "clean": "rm -rf dist",
    "prepare": "npm run build",
    "example": "ts-node examples/kline.ts",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "tushare",
    "api",
    "stock",
    "typescript",
    "kline"
  ],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^20.0.0",
    "ts-node": "^10.9.2",
    "typescript": "^5.0.0"
  },
  "files": [
    "dist",
    "README.md"
  ],
  "dependencies": {
    "dotenv": "^16.4.5"
  }
}
